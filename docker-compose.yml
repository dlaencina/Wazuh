version: '3.8'

volumes:
  wazuh_certs:
  wazuh_indexer_data:
  wazuh_manager_data:
  wazuh_dashboard_config:

services:
  wazuh.init:
    image: wazuh/wazuh-certs-generator:0.0.1
    hostname: wazuh.init
    user: "0:0"
    volumes:
      - wazuh_certs:/certs
    entrypoint:
      - /bin/sh
      - -c
      - |
        mkdir -p /config
        printf "instances:\n  - name: wazuh.indexer\n    ip:\n      - 127.0.0.1\n  - name: wazuh.manager\n  - name: wazuh.dashboard\n" > /config/instances.yml
        /entrypoint.sh
        chmod -R 777 /certs
    networks:
      - wazuh_net

  wazuh.indexer:
    image: wazuh/wazuh-indexer:4.8.0
    hostname: wazuh.indexer
    restart: always
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
      # FORZAMOS LA INICIALIZACIÓN AUTOMÁTICA
      - "DISABLE_INSTALL_DEMO_CONFIG=false"
      - "DISABLE_SECURITY_PLUGIN=false"
    volumes:
      - wazuh_indexer_data:/var/lib/wazuh-indexer
      - wazuh_certs:/usr/share/wazuh-indexer/certs
    networks:
      - wazuh_net

  wazuh.manager:
    image: wazuh/wazuh-manager:4.8.0
    hostname: wazuh.manager
    restart: always
    environment:
      - INDEXER_URL=https://wazuh.indexer:9200
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=admin
      - SSL_CERTIFICATE_AUTHORITIES=/etc/ssl/root-ca.pem
      - SSL_CERTIFICATE=/etc/ssl/wazuh.manager/wazuh.manager.pem
      - SSL_KEY=/etc/ssl/wazuh.manager/wazuh.manager.key
      - API_PASSWORD=admin
    volumes:
      - wazuh_manager_data:/var/ossec
      - wazuh_certs:/etc/ssl
    networks:
      - wazuh_net

  wazuh.dashboard:
    image: wazuh/wazuh-dashboard:4.8.0
    hostname: wazuh.dashboard
    restart: always
    environment:
      - INDEXER_USERNAME=admin
      - INDEXER_PASSWORD=admin
      - WAZUH_API_URL=https://wazuh.manager
      - DASHBOARD_USERNAME=kibanaserver
      - DASHBOARD_PASSWORD=kibanaserver
    volumes:
      - wazuh_certs:/usr/share/wazuh-dashboard/certs
      - wazuh_dashboard_config:/usr/share/wazuh-dashboard/data/wazuh/config
    networks:
      - wazuh_net

networks:
  wazuh_net:
    driver: bridge
